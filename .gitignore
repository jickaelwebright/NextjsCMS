# deps
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# next.js
# /.next/ — committed for cPanel deploy (no build step on server)
/out/

# production
/build

# db files (contain client data)
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm

# uploads (user content, not in repo)
/public/uploads

# env
.env.local
.env*.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
*.pem

# typescript
*.tsbuildinfo
next-env.d.ts
